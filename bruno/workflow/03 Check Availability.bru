meta {
  name: 03 - Check Availability (should be available)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/books/{{bookId}}/availability
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.available: eq true
}
